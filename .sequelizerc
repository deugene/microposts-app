const resolve = require('path').resolve;

module.exports = {
  'config': resolve('./server/config', 'config.json'),
  'models-path': resolve('./server/models'),
  'seeders-path': resolve('./server/seeders'),
  'migrations-path': resolve('./server/migrations')
};
